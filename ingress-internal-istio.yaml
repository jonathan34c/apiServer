apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: rp-internal-gateway
spec:
  selector:
    istio: aks-istio-ingressgateway-internal
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: rp-vs-internal
spec:
  hosts:
  - "*"
  gateways:
  - rp-internal-gateway
  http:
  - match:
    - uri:
        exact: /rp
    route:
    - destination:
        host: rp
        port:
          number: 9080